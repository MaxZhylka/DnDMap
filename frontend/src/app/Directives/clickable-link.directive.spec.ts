import { ClickableLinkDirective } from './clickable-link.directive';

describe('ClickableLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickableLinkDirective();
    expect(directive).toBeTruthy();
  });
});
